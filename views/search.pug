extends layouts/main
include mixins/videoBlock

block content   
    h3 "#{searchingBy}"로 검색한 결과입니다. 
    .videos
        each item in videosData
                +videoBlock({                
                    title:item.title,
                    src:item.videoFile,
                    views:item.views
                })