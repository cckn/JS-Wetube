header.header
    .header__column
        a(href=routes.home)
            i.fab.fa-youtube.fa-7x
    .header__column
        form(action=routes.search, method="get")
            input(type="text", name="term", placeholder=`검색어를 입력해주세요.`)
    .header__column
        ul
            li 
                a(href=routes.join) Join
            li 
                a(href=routes.login) Log in
